import { PostCard } from "../components";
import { PostForm, Container } from "../components";

function AddPost() {
  return (
    <div className="py-8">
      <PostForm />
      :let arrayIndex = array.length while (arrayIndex--) {}
    </div>
  );
}
export default AddPost;
